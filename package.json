{
  "name": "wroom",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Writer's Room — a fiction studio disguised as a social network. One author, many identities. Native-first (Expo) app over a shared core and a Cloudflare Worker API.",
  "scripts": {
    "ios": "npm --prefix mobile run ios",
    "start": "npm --prefix mobile start",
    "typecheck": "npm --prefix mobile run typecheck",
    "server:dev": "npm --prefix server run dev",
    "server:deploy": "npm --prefix server run deploy",
    "server:migrate:local": "npm --prefix server run migrate:local",
    "server:migrate:remote": "npm --prefix server run migrate:remote"
  }
}
